services:
  - type: web
    name: nazenani-camera
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --timeout 60
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
    plan: free
